<!--Cleaning the church-->
<h2 id="{{templatename}}_{{order}}">{{title}}</h2>
<table>
	<tr>
	{{#each dates}}
		<th>{{this}}</th>
	{{/each}}
	</tr>
	
	{{#each names}}
	<tr>
		{{#each this}}
			<td>{{this}}</td>
		{{/each}}
	</tr>
	{{/each}}
</table>
